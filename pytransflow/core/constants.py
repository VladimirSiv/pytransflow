"""Module that defines Transflow constants"""

from enum import Enum


class Constants(Enum):
    """Defines Transflow constants"""

    PATH_SEPARATOR = "/"
    SCHEMAS_PATH = "schemas"
    FLOWS_PATH = "flows"
    DEFAULT_DATASET_NAME = "default"
